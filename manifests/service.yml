apiVersion: v1
kind: Service
metadata:
    name: githubcontainerwebappwithnodejsdemo
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: githubcontainerwebappwithnodejsdemo